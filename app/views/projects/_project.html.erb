<% @projects.each do |project| %>
<div class="project">
<%= link_to project.name, project_path(project.id) %>
<%= project.description %><br>
<%= project.status %><br>
<%= project.location %>
<% end %>
</div>
