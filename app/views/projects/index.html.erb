<h1> All Projects </h1>

<div id="search_bar" class="row">
  <div class="small-12 columns">
    <%= form_tag projects_path, id: 'search_form', method: :get do %>
      <div class="row collapse">
        <div class="small-8 medium-5 large-4 columns">
          <%= text_field_tag :search, params[:search], placeholder: "Find projects" %>
        </div>
        <div class="small-4 medium-3 large-2 columns">
          <%= submit_tag 'Search', class: "button postfix alert" %>
        </div>
        <% if current_user %>
        <div class="small-12 medium-3 columns right">
          <%= link_to "Add a Project", new_project_path, class: "button postfix expand" %>
        </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>

<!-- <div class="row">
  <div class="medium-4 medium-centered columns pagination">
    <%#= paginate @projects %>
  </div>
</div> -->

<div id="projects" class="row">
    <%= render @projects %>
</div>

<div class="row">
  <div class="medium-12 medium-centered columns pagination">
    <%= paginate @projects %>
  </div>
</div>
