<% @projects.each do |project| %>
<div class="project">
<%= link_to project.name, project_path(@project) %>
<%= project.description %><br>
<%= project.status %><br>
<%= project.location %>
<% end %>
</div>
